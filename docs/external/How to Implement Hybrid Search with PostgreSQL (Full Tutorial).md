in this video we're going to build an advanced rack pipeline using just a postgress SQL database we're going to implement a hybrid search together with a reranking this will allow us to build a system where we can leverage a semantic search using embeddings and factors together with keyword-based search methods that we can Implement directly into postgress combining these two together and then applying a re ranking algorithm will allow us to create a really powerful rack solution that has been shown to work better than just semantic search with factors alone so throughout this tutorial I will be walking you through this repository which I will also make aailable for you and of course we're also going to really dive into the code showing you all the examples of how you can set this up now this tutorial is going to build on top of a previous tutorial that I did on how you can set up a postgress SQL database for uh for using rack essentially without needing a dedicated Factor database so while it's not necess necessarily needed to watch this tutorial first it will give you a lot of context on the mechanisms that we use to set things up so I do recommend checking it out first but just know you can also follow just this tutorial and then maybe later check if you just want to fill in some gaps in uh let's say how PG Factory the extension that we're using works for this so in order to follow along for this tutorial you need a Docker installation you need python installed on your system you need an open AI API key you need a postle gooey client so something that you can load your your database in and addition optionally you need a coare API key if you want to implement the reranking as well this is optional you can get a free key I'm also actually using a free key right now just to go to uh the coh here website and set it up now here are the five high level steps that we are going to walk through in this tutorial I put a lot of effort already into the repos story so it's going to be really straightforward to get this up and running and then it's really up to you to take all of these components and tailor it really to your use case so we're going to set up the docker environment which is as simple as running the docker compos connect to the database create a new python environment install the requirements and then we're going to run essentially two scripts that we can play with so really easy right all right so let's come over to the uh repository over here which I have opened up in my cursor IDE which is my favorite code editor right now um if you haven't tried it yet please please do so I also got a video on that I will link it in the description but it's insane what you what you can do with it so the first thing that we need to do is we have this Docker compose file over here and we need to run that now within cursor or within Visual Studio code what you can do is you can right click and you can do a Docker compose up so it might already be running on my system there we go so now it's running so if you don't have an IDE that supports that what you can also do is simply run the command Ducker compose up with the detached flag to run it uh make sure you do this in the docker folder where the docker compost. yo file is so you could also run Docker compost upd and probably it will say yeah this is already running right now okay so that's the first step so what we essentially do right now if we look at the do compos file what's in here we basically take the time scale post SQL uh image and this is what I explained in my previous tutorial but this will set up a post SQL database on the default board with some default settings and it will out of the box install some extensions that are and settings that are needed to perform similarity search and work with uh vectors within postle again more on this in the other tutorial but that's really step one so now we have a database so what we can now do is we can connect to it to uh make sure that everything is up and running correctly so I can come in here so I use table plus let me make this a little bit bigger and I have the local one over here and when you connect to this database using the default settings so you can see that over here it's all Local Host default settings you can uh connect to the database for me there's already table in there but for you this should be empty but just make sure you have that uh connection ready all right and then coming back to the next step we just follow everything over here we're now going to create a new virtual environment and pip install the requirements.txt in here so let me close this out over here so again um I'm using cursor or Fiasco that would be the same so I would come to the command pallet um and I would search for essentially environment create environment and then you can either do a virtual environment or a cond environment and then um we can I already have one here but you can create a virtual environment and make sure you install all of these requirements pretty self-explanatory the fact that you are here looking into advanced rack pipelines I am assuming that you already have basic knowledge of how you uh should set this up now okay now we essentially have everything ready so we have our environment we have our database now it's time to actually do something with this so let's come over to the insert documents file so we can come over here to the app insert factors so what we are going to do over here is we're going to use a public data set from the data sets uh pip uh library that you can use in Python so you can pip install it and we're going to use the C n Daily Mail data sets it's it got like 280,000 news articles or so pretty short articles that we're going to use we're going to create a selection of just 1,000 articles so we have a pretty like chunky uh database to to work with but not so large um that is going to slow us down for this tutorial so that is what we're going to do over here so make sure that before you run this you also do one more step where you uh essentially take a or you make a copy of the example. EnV and you create this EnV file and fill in your openai API key that's that one is already needed within this process like I said the co here not needed right now but in the next step for reranking but we do need the open AI API key because we are going to use open Ai and Bets you can also swap that with with something else but just so you know we need to plug that in as listed here in the instructions as well so once you have all of that up up and running I am going to essentially create an instance of the vector store that I have created to essentially help with all of this we created this Vector store in the previous tutorial but I now extended it with the hybrid search and rer ranking capabilities but for now we're just going to look at what's going on over here so I'm going to load the data set so this is going to take some time because it's a big data set but here you can see we now have that data frame so I take the the articles I select I select thousand articles and I put it into a data frame so here you can now see the data set that we're working with article highlights and an ID now throughout this example this is what we will be using but this could be any other data set this could be your company company documents this could be your internal knowledge base this could be your customer care guidelines whatever system you're building where you want to introduce rack of course you can swap this out just know that then you have have would have to change the structure of the columns etc for the purpose of this demonstration we are going only going to look at the article column and we're not going to do anything with the highlights all right so now that we got the data we can run this function over here and we can apply it to the data frame so essentially how this is structured this is a a fun a function there where if we have a pandas data frame we can use the apply and this will be applied to all rows and what is essentially does it will take the article column which we have over here and it will create a vector embedding and if we can you can look into the get embedding to see what's going on over here this is all in the vector store Clause but it's a simple call to open AI to create the embedding and then once we have the embedding we create a PD series and we add an ID which is a you U ID based on the daytime we add some metadata also Time created then we have the content which is going to be the article and we have the embedding so that is essentially what this function does and now if we select this and run this this is going to take some time because now it's going to run this and and and essentially create embeddings for all the 1,000 articles we can probably speed this up with some more clever ways async methods but for now I've just use a simple straightforward way to go through this all right so that took a couple of minutes but it's completed right now and we can look at the data frame over here we can see we have 1,000 records we have an ID we have some metadata we have the content and we have the embedding so this right now is the perfect setup to push this to a vector database or in this case a postgress SQL database with the PG Factor scale extension enabled and that is essentially what we're going to do with the following commands over here again I explained all of this in the previous tutorial but we can go ahead and create the table so when we do this uh I just deleted it to give you uh an example so you you can run this and it will create the documents table for you as you can see right now it is empty so if the ID metadata cont embedding exactly matching our data frame then we can create the index so this is the search index for the vector search so for the semantic search and this is uh built on top of the PG Factor scale extension so you won't see anything but just in the background within the database we add this index and now we're also going to add a gin index gin to help us with the keyword search so this is also what we are going to index and now we are going to run the upsurt and this will essentially take this whole data frame and push it to the database so this is going to take a little bit as well so we're going to run this and there it is finished so we can come back over here reload this and now now we can see we have a beautiful table over here with everything that we need in order to proceed with this tutorial so now coming back to the let's see where are we the giup repository and the instructions we have now uh run the insert vectors. P so now we can go ahead and switch to the other file which is search.by so throughout the remainder of this tutorial we will be walking and playing uh through this file over here to show you the difference really between semantic search keyword search and then the hybrid search and then combining everything with reranking because that's really the goal uh of this tutorial to show you how to do this how to do all of this with just a postgress SQL database now one thing that is also important to mention everything out of the box if you follow this tutorial U it will work but you can have a look at these into the settings.py to get a little bit more insight on how this project is structured so this is using ptic settings which I like to use to have one central place where I can figure things like API Keys database settings and here you can also for example see that the table name is documents and that we use here are the dimensions for the embeddings that currently match with uh openi the text embedding uh small model so just so you know that is all here and if you for example want a different table name it's all configured in here all right so now let's go to the the search. py file over here and boot this up initialize the vector store and we have a simple query here is there any news related to London so again remember remember we have 1,000 news articles within our database from CNN this covers Global News and we're going to figure out is there anything about London why London I have no idea it is pretty arbitrary uh because really looking into the the data itself and validating that is is for another tutorial this is just going to show you the mechanisms so what we can first do is we can do a semantic search so this is all covered in the previous tutorial but we can run this using the vector store you can have a look in here so this is all within the code base you can just see okay santic search what does it do and it essentially leverages the time uh scale factor client from time scale uh the company they have created this and it's really a convenient like helper library to perform semantic search similarity search on a database using a postgress SQL so we can look at the results over here and we have the query and we have a limit of five so here you can see we got all of the results over here so this is basic rack right this is probably if you watching this video this you should already be familiar with this concept basic rack semantic search we use cosine uh similarity and we calculate the distance all right but now the next step that I've introduced is keyword based search so what we can do over here is we can search the same database the same d uh data but instead of doing a similarity search on the embeddings we're going to look at the contents column and we're going to leverage some of postgress sqls built in search capabilities so if I come over here I am not going to explain in technical depth how this works because it's much better if you really want to understand this to read through this documentation and then also follow the link essentially to the official postest SQL documentation so there's a little bit of context over here on how to set this up but out of the book poster SQL has some some pretty powerful uh keyword based search mechanisms that we are going to use and we can do that uh simply using the SQL query language so that is exactly what is implemented here if we look behind the keyword search so you can see we have just a SQL statement over here where we take the ID content and then we use the uh TS rank CD and this is poster Seal's way of ranking uh the results in order to get the score and then to get results back so what does it look like well it looks exactly like a semantic search but now with a keyword-based search and we also get results back and here you can see we put the limit to five but we only get two results back and one important thing to note this is both the the the strength and the weakness of keywords based search it is um really strict meaning that there's no fuzzy searching or anything this is only going to retrieve documents that contain all of these words minus probably the stop words so I'm not as 100% sure how this works behind the scenes but we uh behind the scene but we are working uh to TS factor and we specify the English uh language so it's it's going to do some pre-processing based on the English language in order to help us with keyword retrieval but in general what you can assume is that it will only match for in this case articles that contain all of these like big keyword so is and and any and there are probably like or two are probably like stop wordss that are filtered but news London it should contain that so that's why it is much more restrictive but the real use case here is especially especially when you want to search for really specific terms within some context so for example it's really important for example within a company you have some like some uh abbreviation or codes or specific technical keywords that are really important with just semantic search alone it can sometimes miss those whereas um with keyword based search it is going to create an exact match on top of that so what we have right now as a result we have two ways of gathering results from the database semantic search keyword-based search what we can now do is we can also combine the two so the hybrid search method that is implemented over here is nothing more than first doing keyword search then doing the semantic search combining the results and removing the duplicate IDs so and we just keep the first so in in order to like save tokens and compute and processing power we we want to get everything from both sides combine them together but then uh remove the duplicates so what that looks like is if we run this you can see over here that right now I've set the key uh you can specify the the case or amount of results that you retrieve for both keyword and semantic search I've both set it to 10 and here you can see that semantic search if you put in KS 10 and there are more than 10 10 records in the data set it will always always retrieve 10 records but as we've seen with the keyword based search this will limit the search in this case to two to the two records containing uh all of these relevant keywords so then combining all of that together you can see that the first two results are from the keyword based search and then we have uh a couple more that are from the semantic search and now the interesting thing here is that we already know that the keyword based search will return two records right so we have the the first two over here and the semantic search will return 10 records um which should give us a total of 12 records retrieved but then it should also do the D duplication right so there shouldn't be any duplicate records in here but now the interesting thing is if we look at this we can see that we have 12 records in total so you can see Zero index and it goes up until 11 so this means that in this case the keyword based search got two different results that were not retrieved by the semantic search now whether this is right or wrong or effective um I'm not sure for this use case but it also doesn't really matter for the sake of this demonstration it just goes to show that by combining these two different methods we can essentially create a wider net of potential contact that we can retrieve from from our Factor database we can essentially create a wider net of things that we can retrieve from our Factor database so now we have our results over here so these are our hybrid results so now if you would follow a a regular like rag approach right so these are your results so in this case we have 12 records we would then structure our prompt right we have the user query and we combine the two send it to a large language model and would then let the model figure this out the problem right now is that this is still a pretty naive ordering because we first did the keyword-based uh search and it will put that on top so you can see two keywords here and then it does all of the semantic searches over here where within the different search types these are ranked according to their score so meaning the first record over here from a semantic perspective is more similar to the query Than This Record over here and same is true for keyword so this is probably more relevant than this one but it's not ordered within this column itself so not within the entire results because it uses a different metric to compare it right this is the cosine uh distance that we're using and this is the uh post sqls TS rank CD relevant scoring so we can really compare the two it's not comparing Apples to Apples over here so this is where the final step comes in and this is where we can perform a technique that is called reranking and with reranking we essentially use a large language model to look at all of these results also combined with the query and then do an additional ranking in here and with that we can also then again specify a uh top n in this case to say we okay we want to look at all the results we want to rerank it we want to order it and then just take the top five and to get more context on how this works let me scroll down a little bit further over here so we're using caires reranking model right now so I've heard some good things about this uh there was also an article from entropic stating stating about how they they got really good results with this reranking method so you can read the block post to get a better understanding of what's going on and also look through uh the documentation over here but what we can do over here is if we look at the Hybrid search function there is a uh parameter in here that's called rerank uh it's a bullion it's defaults to false so if I now come back over here you can see rerank over here is true and we also specify the top five we can let's see run this one more time and as you can see this is really fast so this is performing the semantic search the keyword based search and also the reranking and then if we look at the ranked results we can now see that we only have five results and you can also see that all of the five records are from the semantic search type so in this case for this specific query the large language model identified that the results extru using the keyword-based search were not as relevant as the semantic ones but again like I said this is going to really depend on on your use case and one other potential step or technique that you can introduce in a system like this which is really straightforward is instead of sending this query directly to the uh keyword search is you can put another processing step in between where you take this query and extract keywords from it and then run a search for all of these keywords individually you could for example set something up very easily using instructor or open AI structured output and for example specify take this query and extract the two most relevant keywords from it and then that is going to be your input so then the query would be more of like an actual keyword that you put in here and you could do this iteratively and combine all of these results and then bring it to the reranking so again showing you all the context but also letting you think about okay how would I take this and implement it into a system that makes sense for my use case so now then once we have those ranked results what we can do is we can call the synthesizer and we can run this and this is a simple CL this is a simple uh this is a class with with a method in here to essentially take the results retrieved within a rack system and then combine it with a large language model to synthesize the response so again you can look into the code but more uh on this is covered in the previous tutorial and here you can see it gives a summary recent news related to London includes several topics so it gives a bullet list and here is all the information that we retrieved from the system so this really is endtoend rack right here with pretty Advanced capabilities right we went all the way from taking the Articles creating the embeddings putting it into the database uh retrieving it using uh semantic search then also extending that with keyword search bringing that together in a hybrid search and then also reranking those so these really are really powerful strategies right now that are used in uh the best rack systems that you see in applications deployed right now now one more good thing to note this is also I got some comments on my previous video about the bm25 ranking which is currently really the state-of-the-art to do keyword-based search within systems like this and this is also typically what you see when people talk about hybrid search and it is not directly supported within postgress SQL but there are workarounds for it I haven't implemented this yet but there are extensions for example like PG search that implement this so if it's really crucial for your application you might consider looking into this but this will already go a long way for most applications and with that we have covered all the steps that I wanted to cover in this uh tutorial you can reference the repository over here for more information and also some further reading and I also invite you to look at this code based and look at this repository specifically the factor store to really understand how this works behind the scenes so you fully understand how you can take all of these components most of them really are open source with the exception of two API keys that we need but other than that it's fully open source so it's really powerful you so you can take this and implement it into any system that you are building really regardless of where you are deploying whether that's locally on Azure AWS or on Google it doesn't matter you can take this and you can implement it put it into Docker put it on a server or a container instance and and you can run this so that's really what I like about this setup about being completely in control without relying on uh external Factor databases which do have uh their place but for me the project that I'm working on I love this setup also mostly because I can just have one single database for my application so I can just come in here I have a for example a documents table over here with all of the embedded and next to that I can also for example log all my events all my records in just another table so it just makes the whole process a lot easier that's why I like this setup um and it's really powerful especially when combined with these more advanced rack techniques as we've covered in this video all right and that brings us to the end of this video now the fact that you are still here probably means that you have a pretty good understanding of how to work with data and AI because this is pretty advanced stuff and you might want to learn is right now for the projects you work on in your company or you're still learning and I just want to put out quickly that if you're a data professional you already have some experience and you consider also potentially doing this as a freelancer meaning you can actually sell these Services through your own company to help clients but you don't really know where to start or you struggle to find your first clients then you might want to check out the first link in the description it's a video of me going over how my company can help you with this now in all transparen currency this is a it's a funnel page it's designed to get leads for my company so just so you know that but we have a program called data freelancer and it's specifically designed to solve this problem to help you get started with freelancing now then I want to thank you as always for watching if you found this video helpful please leave a like and also consider subscribing to the channel if you want to see more technical tutorials like this and then to wrap it up as your next action I highly recommend checking out this video next it's a video of me going over the entire process C that we use inside data Lumina my company to find deliver and build generative AI projects it contains some positioning some some sales and also some architectural strategies that we use so make sure to check out this next