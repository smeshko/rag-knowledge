In this tutorial, we're going to build a hybrid retrieval system from scratch that combines BM25, dense embeddings, reciprocal rank fusion, and a re-ranker to then bring it all together. This is really the production stack that works and that wins in 2026 when you want to build serious production ready systems. By now almost everyone kind of like knows how to build rack systems and they've tabbled with it and know what a factor database is but almost no one understands how to build them from scratch and optimize them and evaluate them for your own data set which is exactly what we're going to do in this video. And now a quick word of caution before we dive into things because this tutorial is not going to be like most other rack tutorials that you'll find online and on YouTube. In this one we go really deep. This is really for engineers, developers who already have a basic understanding of what rack is and need to use this technology really professionally within their jobs or the clients you're working for. And you want to build a system that you truly own and understand. All right, so let's kick things off. And the first thing that we're going to look into is the actual data set that we are going to use because for any retrieval task, the data is the most important part. And that's really where we should start. So we're going to use this data set, the financial QA data set, which is also part of the bear benchmarks. So this is a benchmark for information retrieval. So let's quickly cover that first because this is very important to understand because whenever you start with a new data set, you ideally want to have some kind of like ground truth to decide what makes a good retrieval system because otherwise we cannot optimize, right? So what's nice about the beer data sets is that they created these pairs of queries, relationships, and corpuses. So as we go through the codebase and go through the examples, this will become more and more clear. But for now, you should understand that the data set that we are going to use consists of a whole bunch of financial questions and statements that are linked together. So in the corpus there are answers and these are similar to your own documents that you would for example put in your retrieval system whether they are company documents, your sales playbook, uh customer support questions, pretty much anything that you want to put in your corpus. That's a starting point. Now then also we have questions that can be answered using documents within that corpus. So let's actually check what this looks like in the codebase so we can actually get a feel for this. So I am currently in the hybrid retrieval folder. Then under data there is a download finance QA.py file. So this is a one-time setup that you can run to download the corpus. So you'll need to set up your project. So I have a pi project atomal over here. I recommend to run uv sync in order to get this all up and running. We are using the data sets library here from hugging face. And because of that we can simply run this and it will load all of these data sets. So we got the corpus, the queries and the relationships. Remember how I was talking about this over here. So this is a one-time run that you can do. I already have them downloaded over here. So for me it will probably skip things. So just run that and then it will store that data into uh parquet files. So this is a very efficient way to store this data on disk that we can then also later load in. So that's the data set. But now let's actually get to the exploration phase. So I'm going to the one explore data file and what I'm going to do is I'm going to use the Jupyter interactive session here on the side so we can walk through it step by step. If you don't know how to do this, I'll link another video below to explain exactly how I set up my cursor to work like this. Uh similar for Visual Studio Code. So we're starting over here with our data folder that we just downloaded that data to. Right? So here you can see the full path to it. Okay. And now we're going to load the paret files that we just downloaded to get a feel for what this actually looks like. So if I now look at the corpus, you can see this is currently a pandas data frame. So if you're if you've never worked with pandas, then uh this is pretty much an Excel spreadsheet, but then within the Python programming language. So you can see the table over here. This is also why I like using the Jupyter interactive session because it renders these tables really nicely. So you can see we have a whole bunch of rows, a whole bunch of documents and there are three columns each uh containing an ID title is empty in this case. This is a default for the beer data sets but this particular finance QA uh set doesn't have titles. So we just have the text. Now let's look at the first element in here to get a feel for what the corpus looks like. What data are we actually working with? So I'm using some pandas notation over here where I'm going to say look I'm going to take the text column. So that would be corpus and then the text which you can see over here and then I'm using the eyelock zero notation over here to say let's now just look at the first element in there. So let's see let me actually if I clear this for you and run this one more time. So this is the first document that shows up in the corpus. And on its own, it doesn't really say much to us right now. So let's actually continue with the queries. So now I'm going to load the other parquet file which is the queries. So let's run that. And you can see that now this also results in another data frame. Exactly the same structure. We got an ID, title is empty. And now we got we got some text in here. So let's again look at the first uh query that is in there. So if I print this over here, you can see right now instead of a document with information in it, we have a question. So what is considered a business expense on a business trip? And you can also kind of like iterate through this to see okay, what are what is actually in here? Starting a new business online. Let's pick some random ones. New business owner, how did Texas work for the business versus individual? So you can see these are the actual queries that show up. Now then the third data set that we have in here is the QRLs which is the relationships. Now this is where it gets interesting because here we create a mapping between a query ID and a corpus ID. So if I just look at the first record over here you can simply say that see that we we have query ID 8. So question 8 within that uh query list relates to or has a relationship to the document with the following ID and that is indicated by the score is one. So what you'll find is that in this QRLs all of the scores are one. The ones with a zero are not in there. So these are all of our relationships. Now this is you need to spend some time here to really understand this and that's why I also created this image for you to dive a little bit deeper into that because everything that we're going to do later down the line comes back to documents queries and the relationships between them so that when we run the retrieval system we can see when we ask this question do does our retrieval system does our hybrid system come forward with the right documents that have a relationship with the query. So make sure you really understand this and then also understand how you can map this back to the codebase because this is really the mental model that you need in order to go forward. So then step number two of this first file is we're going to make a simple filter on all of the queries because if we look at currently all of the queries that we have you can see this is a big list over here where we have a we have a lot of queries but not for every query we have a document associated to it within this data set that we are using. So this is a simple filter that we do where we take the QRLs. So let's see we take the ids over here. So these are all the questions that we have and we take the set which is Python's way to dduplicate a list or um a series in this case. So here we have the dduplicated set of that and then we just do a filter on the test queries. And again this is a little bit tricky to understand if you've never worked with pandas but pretty much what we do look if I run this we now have a filtered list. So you can see that we went from how many did we had? We had 1,700 to start with and now we have 648 left. So that's just a filtering mechanism that we do over here. And then to show you one more thing, if we inspect one query and its relevant documents, here is how you can see how everything comes together. So let's say we have the following query. So now we have the the first test query over here. And if I print that here, you can see what that looks like. So this is where should I park my rainy day/emergency funds. So this is the first query that we now have in the data set where we also have documents backing that up with the relationship. What we then do is again pandas notation. We go into the QRL's data frame and we perform a search to check for the query. And this is similar to doing a lookup or a SQL where statement. And now you can see that for the query where should I park my rainy day emergency funds which has this ID. You can see that if we apply a filter we get the document ids. So bringing this all together, we can now see that this particular query with this ID links to these documents, meaning this question can be answered through these documents, which is exactly what was shown in the image over here. Okay, so now that you understand the data set and also know how to work with this locally, we're going to dive into the actual retrieval pipeline and we're going to work through this step by step. So first we'll get into BM25 then the embeddings then RF ranking and then bringing everything together. So that's going to be the flow of the video building each and every individual component first and then bringing them together later in the codebase to form one cohesive pipeline. So, we're going to start with BM25, which is a keyword-based way for information retrieval. And with all of the methods that we're going to cover, I'm going to walk through the codebase. I'll explain as much as I can during the video itself. But also what I want you to know is that there is a docs folder and for each and every one of the techniques there is a additional markdown document with all of the formulas and the math behind it as well if you want to go deeper as well as the original papers where all of these techniques came from. But I cannot go into all of the details otherwise this would literally become like a 5 to 10 hour lecture. But I'm just going to give you what you need in order to follow along. And then again if you want to go deeper the information is there for you. So BM25 also known as sparse retrieval. More on that in a bit. We'll do keyword overlaps and then there is a waiting mechanism that is applied to it to actually perform the math to see what we need to retrieve. This is really good for catching exact terms, identifiers, rare words, but it will miss paraphrases. So this is a rather naive way of searching and retrieving information but in some scenarios can work highly effective. So let's dive into this first and see what it looks like. So if I come back to my codebase and then jump into BM25.py, we have the setup over here and we are going to use the following library. Let me actually open that up for you. So this is a Python BM25 implementation. So this is what we will be using. This is also a dependency in the project. So pi projectl you can see bm25s. So now you know where this import is coming from. And the nice thing about this implementation is that we don't have to worry about the mathematics. We can just rely on what was already built and use this. So we are going to start with loading our data directory again. So this is the same one we were using. And now we're also going to create an index directory. So let me show you over here. There is an indexes folder over here where we are going to store the information. So we are going to use that later. So for now it's only the paths that are in here. So now we're going to simply read our corpus again. Similar to what we already did, right? So remember this is the data frame. And now we're going to convert it to a different data format to make it work with the BM25s. So we're going to create lists out of it. So what you can see right now is we'll use pandas to list. So we can get the doc ids which is just a list of all the ids. And then we also get the text and we do the same. So now we've converted. So you can see this is just a huge list. So now we convert simply from a data frame to lists and we put all of the documents in there because we want to create keep the corpus the original size. Right? So remember how we made a filter on the questions the corpus should be really big because otherwise we kind of like cheat on the retrieval task. Right? Because if we only leave in the actual relevant documents, we just reduce the size because the more data there is, the more errors a retriever can potentially make to retrieve nonsense. Okay, now we're going to tokenize the entire corpus. And this now becomes pretty easy because we can use the BM 25S library for this. So simply what we do is we call the tokenize method. we put the list of documents in there and we can also filter for English stop words. So because it's a keyword-based mechanism and it will create calculations and a score really on how frequent the words are occuring in certain documents etc. Removing stopboards can help because these are generally noise. So if we now convert that to tokens you can see that process takes 2 seconds or so. And now if I look in here, you can see we get the following tokenized data over here. But let's actually figure out okay, what is this? I see some ids. I see a vocabulary. But this is all very messy to me still right now. So let's actually have a look in that. So what essentially token tokenize does, it's it creates a tupole of token IDs and vocabulary items. And if we look at the first element in the tokens, you can see this is now you it creates one inner list per document. So this is the first document in our corpus. But now with simply these ids and these ids the they reference to the vocabulary. So if I now look into the vocabulary which is a dictionary with a string and then an integer you can see there is a lookup over here right now. And how you can read this is as following. It's simply a mapping. So because this is the actual first document in our corpus, you can see that it starts very sequentially, right? So we have 0 1 2 3 4 5. And so you might ask yourself, okay, why is this all the same? But then here you can see for example that we have five again. So five is over here. So we can see what is five? Five is training. So you can see that this is now just a representation of how to structure a document using IDs and a lookup vocab vocabulary table. That's how this is set up. So again, you might need to play with this a little bit to figure figure things out. If I now take the second element over here, you can also see that right now the the ordering is going to be completely different, right? And you can always like link it back to the original item in the in the corpus. So for example, if we are over here remember right. So let's say we have the corpus and we take the we take the text out of that. So this is just the same exercise and we take the log one. You can see that over here this is the first document. So it removes the stopboard. So here you can see for example you see training and then here is training one more time. So that is how this is the five over here and the five over here reference training and we can see that in the document over here. So you can see this is something that you just have to go through. You have to kind of like play with this a little bit for it to start making sense. And that's why in this tutorial, I want to just spend some time to just go deep into it because especially nowadays with AI, it's so easy to just kind of like oneshot these systems and oh yeah, just use a BM25, do a hybrid system, and then just like let it run without actually knowing what's going on behind the scenes. And there's just no way to understand that without tinkering with this and actually just looking at those IDs and then making those matches. So we created the tokens and now the next step in here is to create the retriever. So now we can again just use the BM25S library. Very simple. We have a retriever and then we say retriever.index and we plug in the tokens. So this is now how we create the retriever which is ready and loaded locked and loaded with our own data. Now it's also one one thing to worth mentioning here is that BM25 also has different uh implementations different methods. So you can see that the default method is the lucine in here which is also indicated. Now again if you want to go deeper into this here is how the calculations behind this work as well as uh additional further readings and resources that you can dive into. Now the next thing that we're going to do is we're going to simply persist this index to the disk. So we're going to create a directory and then we call can call the retriever safe and then we just simply put uh a path in there of where we want to store our index and then we write that. So if we run this and I come over here I already had it but you should now see that all of the data all of the information is stored in here. So one of the interesting things about BM25 also is that you don't really need a often need a database for this. This can just live locally on on disk. And if you for example look at right now so let me actually show you the size of this you can see that is 33 mgabytes on disk and this is using a corpus of 57,000 documents although short documents. The interesting thing is that for most use cases out in the wild when people are building these rack systems, these information retrieval systems, your corpus is almost never that big unless you're working on enterprise applications, multi-tenant solutions. If you are automating within your own company, doing this as a freelancer or an agency to help local businesses, your corpus is almost never this big. So those files can easily just live on on a file. You can even put them into version control. You could also see the indexing was really quick, right? It took like one or two seconds to do this. So no database really needed in order to to store this. You just load it in memory and use it. Okay. And then go to step number four, actually running a query. So okay, we created up until this point it was all prep work, right? But this is where we're actually going to do the first step of information retrieval where we take a query and we search it against the entire corpus and then retrieve the document. So here is a implementation of how we do that. And again because we are using the BM25s library it's very simple. We just call the we use the retriever and we call retrieve. So let's walk through this together to see what this looks like. And I'm going to actually take the following query over here. So where should I park my rainy day funds? And then let me also actually put K is 10 in memory over here. So we're going to retrieve the top K is 10 documents. What we then do is we follow the same tokenization process on the query as what we did with the corpus. So this is simply just turning any query into query tokens. So now you can see our question of where should I park my rainy day funds is tokenized as follows. And this removes the stop words and then just gives a token to every word in here. And then here you can see how that is built up because the query starts with where and then shoot and then you can see I is removed from it and then park is number three which is index two. You get the idea. This is how you walk through that tokenization process. And now one thing that's also worth to mention here is that the tokenize is a deterministic mechanism over here that will always yield the same output. So this is not using our customized or trained data at this point because we don't need it. We simply need to tokenize it. Where it does get custom is on the retriever step remember right because we created the index on that. So that's what where our own custom vocabulary and index comes into play. So we take our query tokens that we have and then we run it against the retriever with K is 10 in this case. And then what you'll find is that we get the indices and we get the scores. And now what we do is we simply bring that together in a list. We loop over that. And then let me actually show you what that looks like if I run this all together. So we can get a feel for what this looks like. Let's see over here. Let me actually clean this up and run this one more time. So it's the top. Now you can see that using our BM25 index, if we tokenize where should I park my rainy day funds. These are the actual queries that we retrieve from the corpus. So you'll find the scoring over here. Then the index of the document itself and the actual text. Five documents retrieved. And then real quick, the fact that you're still watching this video signals to me that you're pretty serious about AI engineering. But one of the big problems that we have right now is that no one is really trained to be an AI engineer to work with LLM because everything is so new, right? And there are actually very little courses and resources online that really teach proper AI engineering end to end. And I even ran into this issue in my own company when I tried to hire AI engineers and figured out that actually nobody's properly trained for this. So that's why I created the Genai Accelerator program. This is a six-week track. We've had engineers from booking, Intel, Electronic Arts, even from Meta going through this. And it will teach you in six weeks everything that you really need to know to build production ready AI applications all the way from foundations, system design principles, architecture, rack, of course, LLM monitoring and evaluations, and also very important, deploying your AI applications to the cloud and to actual production environments. And now, we also have a 30-day money back guarantee for this program because we've been optimizing this for over a year already. And literally everybody loves it. So if you don't like it, you can try it out. Literally just shoot me a message and I will happily refund the entire amount for you. And here's what makes this even better. We have officially partnered with Cretley to turn this into an actual certification program. So because of this, you'll also find that a lot of students go through this program via their company. So you can also reach out to your manager and see if this fits within your learning budget. All right. So, if that sounds interesting, make sure to check out the first link in the description. And now, let's get back to the tutorial. So, that is the first step of our hybrid retrieval system, BM25. So, sparse retrieval, keyword overlap with TF style waiting. And it's excellent to catch exact terms, identifiers, and rare words, but it will miss paraphrases. Now, let me scroll a little bit down over here to pretty much explain why each piece earns its place in this retrieval pipeline and why they work so well together. So, for terms, like you can see over here, like the exact specific terms, BM25 is super good, but it's often it it runs into problems where you have paraphrasing, users asking questions in other terms than how they show up in the documents. And that's where we can get into embeddings using dense embeddings. And this is really why it's so nice is dense uh embeddings really win exactly where BM25 loses and loses exactly where BM25 wins. They're kind of like two counterparts to it that work really well together. So let's now look at how we're going to set up this. And if you followed any previous rack tutorial, then you are already familiar with these concepts and it will probably be quite easy to understand. So we're going to walk through the embeddings file over here, the three embeddings.py, and we're going to use text embedding 3 small for this. Now, this requires an OpenAI API key. You can use open-source uh local text embedding models, but for a actual proper retrieval system, I recommend whenever possible, just use one of the better models out there because it will simply improve the results of your system overall. So, we're going to follow a quite similar setup and we'll identify some directories in here and we'll create some simple functions that we can use to create the embeddings. So, you can see we're going to use the openAI client over here and we can use client.bedings.create. and we'll plug in the model which is the text embedding 3 small model which will output a an array of uh 1536 dimensions. So that will be the length of the the vector embedding in there and this is a simple batch function that we can use to loop over this. Now what we are going to do is we're going to read the corpus again. So same structure right we did this also with BM25 but now we're going to take a different approach. So we take the ids, we take that list, and now what I want you to be aware of is that when you're going to run this, since we're going to do it over the entire corpus, this is going to uh incur some costs. So it will be a couple of cents to kind of like run through this. We only have to do this once and then it will be cached because we store it to disk. But I just want to you to be aware of that. Again, for the sake of like following and demonstration, you could also swap this with an open-source embedding model. how to do that like you can literally simply open this in cursor cloud code to say look we're using openAI can you implement an open source embedding model for this and it will set you up the process will be the same but the output that you will and the results that you will get in the end will be different so let's go step by step over here and then zoom out a little bit to understand what's going on behind the scenes so we're again going to take all of the text in here we're simply going to split that up and this is where the magic happens So there's a if statement in here that says look if we already have the embeddings which we're going to store to dense embeddings npy over here. If that file is already in place which you can see is the case on my system because I already ran it. We will simply load it into memory by using mp.load. And if it's not, we'll go over it and we'll build the index which you can see is the function we specified over here. And this will loop over everything. So it will take batches of size 256 and it will loop over the embedding model over here to create those vectors. Now, one step to kind of like zoom out and just for your reference again, if you've never worked with vector embeddings before, you probably want to spend some time to kind of like go over this and understand how factor embeddings work and how we are essentially capturing the semantic meaning in a numerical representation. This is now again the point of this tutorial where I cannot really go deeper into all of this to explain exactly how this works. That's why I added the document over here. What I want you what I want to show you right now is show you how this comes together and how we can use these embeddings. Because if I now simply run everything over here and then clear all of this out, you can see that we now have the embeddings over here. So now we have we have the factors and we can even say just to get a feel for what this is we can take the first one. So this is the the factorized uh version of the first document in our corpus and you can see the exact shape over here right so this is how long the array is and this is because we're using the text embedding 3 small model now one little kind of like tip that I want to give you here is that if you've worked with rack in the past there is almost a 100% certainty that you used a vector database to store your vectors All right. So this I'm assuming that for most people following this tutorial, you are familiar with this process of taking the corpus and then turning them into embeddings and then storing them into a vector database. What we are doing over here, what you can see over here is instead of using a vector database, we're simply using a numpy array. Numpai is a Python library that's uh here you can see it's used for uh computations. uh it's essentially the library that all of like AI and deep learning is pretty much built around because this is what actually stores the uh the matrices and does the matrix multiplications that all like neural networks, machine learning models, any types of models really are based on and we can leverage that really without needing a factor database. We can simply just store these factors to disk and that is what you saw over here. So we have an embeddings.npy npie file over here and where we do that is the uh following. So you can see over here we simply do after we create all of the embeddings. So here you have all of the embeddings over here. In all other rack tutorials, you'll find that okay, now we have the embeddings. Let's put them into any type of factor database. Whether that's Quadrant, Lens DB, WV8, PG factor, let's put it into a factor database. But for any corpus really under I don't know a million of chunks really in there, you don't really need it. Similar to what I explained with the BM25, right? We had like the 30 MB file. Similar with the embeddings, we can also just run this in memory and then use that without the extra complexity. So that's the np.safe that we run over here. We get the npi embeddings file. And let me show you just for the sake of uh clarity, show you how big that file is. So you can see this is bigger than what we had with the BM25. So this is 350 megbytes on disk. But again, nothing too crazy, too large that any deployment, any compute, any VPS cannot handle, right? And again, this is a pretty substantial corpus. We got 56,000 documents in there. So, just a quick little tip to show you that you often don't really need a vector database. So, now let's continue to creating the search again, right? So how do we use this now that we have these these embeddings? How do we use it? So now here we have a function. Let me clean this up for you. A function to perform the search. And what we're going to do in this case, we're again going to take the same query. So we have the query where should I park my rainy day funds. We'll put that K is 10 into memory as well. So we're going to retrieve the top 10 documents and then later show five over here. And now the next step is to create that embedding. So if I run this file over here, you can see that we now turn that where should I park my rainy day fund. We we create the embedding similar to what we did with BM25. Remember we did the tokenization. So we did the prep work. So it is then in a format that we can search against our entire corpus. So we're doing that in the same way over here. But remember here we're making an API call to OpenAI. So we need that model in order to get this uh computation with BM25. We could all do it locally because of the BM25S library. Now here there is one step in there which is for this particular embedding model that we're using. It's not needed but I wanted to show this in here anyway. And that is we perform a normalization. And normalization means that we rescale the factor so that the ukidian distance becomes one in total. Now here is that explained again in more detail but why we include this in here is even though OpenAI if we use uh the embedding models they are all the factors are already normalized so we don't have to do it but you can see we pre-normalize it over here during the embedding process on the corpus and we also do it during the search on the actual query. This is just a safety precaution so that if you use any other embedding model that doesn't automatically uh get you normalized factors, we perform that step. And this is super important because when we're working with normalized factors, we can make the retrieval part easier because again, if you've worked with vectors with rag before, then you've probably heard about cosine similarity, which is how we actually calculate the similarity, the semantic similarity really between two uh two factors within the space that we created. So here's a 2D representation over this. Although in the in the real world it is much more complex as you can see over here. But if we use normalized factors the calculation becomes simpler because we can essentially just take the dot product. It's explained over here. Why that works? Again this is the mathematics behind it. But if we have normalized factors we can simply take now our doc embeddings normalized which is our entire corpus. Remember we can take our query vector which we just created and we can simply use this at notation over here which is numpai's way of calculating the dotproduct. Now what we're essentially doing over here is we're performing a matrix multiplication. So this is the entire matrix over here and we pretty much take every array in here and we multiply it with the query fact and then we get our scores. So now we get all of the scores in here which we can then sort to the top K. So now you can see the following array over here and in the function we will return those particular ids. So again this is quite deep to kind of like get into especially if you want to understand the math behind it. But this is what's going on behind the scenes in most factor databases when you offload your data to that and call the kind of like retrieve or get similarity function. We have now implement that implemented that in almost pure Python really from scratch literally just le the only kind of like thing that we're leveraging over here is the numpy library. So if I now store this into memory and then just run the entire thing you can see that now let me scroll up. We got back to the same as what we had with BM25, right? We had we have a query. We can search that against our corpus and now we in this case I you can see K is five. So it prints the five most relevant documents but now through a different mechanism through a dense search. So you can see the scores, the ids any documents associated with it. So that is step number two of our retrieval pipeline. Right? So remember to zoom out a little bit really kind of like summarize and get a clear understanding of where we are now at. We have two different ways of creating these indexes and then two different retrieval mechanisms so that we can get five documents or 10 documents over here and over here using these exact uh methods over here where we now also know the strengths and the weaknesses of each one exact keyword matches and the exact inverse namely more par paraphrased and actual meaning behind it. So how do we now bring that together? That is step number three and for that we're going to use a technique called reciprocal rank fusion. This is another algorithm and this is I would say the simplest out of everything that I shared if you really want to kind of like understand some of the the the math and the calculations behind it. This one is quite easy to follow. Pretty much what we do is you can see over here where again what I just explained we have two ways of getting rankings. five documents via this, five documents via that. What the RRF essentially now does is it takes it has this formula over here K equals 60C tends to be the kind of like norm that was also introduced. It's the smoothing factor which is was also introduced in the original paper which kind of like still works and everyone defaults to. So you can now see if for this particular document so the green one BM25 ranks it at number one and dense ranks it at number two you can see the formula we do one we divide it by 60 which is the smoothing factor K plus and then the rank so this is for BM25 it's rank number one and here for dense it's number two so this now becomes the calculation and this becomes the score you see it's a little bit small but that is how it's calculated now if you look at the purple dock over here you and see rank number four, rank number one, rank number four, rank number one, do the calculation and then you pretty much sum everything. So this is a way to simply bring together different retrieval algorithms where the original scores cannot be directly compared because it wouldn't make sense to compare the score the similarity score like the cosine similarity of dense ranking and then rank it against BM25 because they're on different scales that would be comparing apples to oranges. So this is a way to say look we just care about the rank and that's how we bring them together. So that's a short little introduction. Again more information original papers can be traced back over here. So let's see what this looks like in the code. If we go to RF file number four you can see that same smoothing factor over here which we start with. And here is the pi the pure Python implementation of what we do. So we pretty much loop over all of uh everything in our list over here and then here this is where we do the calculation. So here we can see that exact kind of one divided by k plus rank right similar to what we did over here that is the pure python implementation. So let's store that in memory and now we can actually move quite quickly over here because right now you know how the BM25 works, how dense works and we have the corpus. So now it's it's all about the ranker and what I did in this step. So you can see in the folder you can see a utils in here and you can see that we are performing some import. So from utils retrievers you can see that I created a let's see the BM25 retriever and the dense retriever. So it's a simple I created two simple classes that we can import so we don't have to kind of like redo the code in all of the files. So this is also like if you want to take this into your own project, import them from the util. Simply take these and put them in uh in whatever you want to use. So okay, coming back to where were we? The RF over here. Yep. Okay. So now we just simply load it. We have everything. And then we define a new function over here to perform the hybrid search because this is we're now bringing them together, right? So you can see we perform a BM25 search and a dense search. Same query, same candidates that we're retrieving. And now what we want to do is because we're bringing things together. We want to bump up K a little bit to say look you can just like just get a lot of documents. You can take top 50 like just show me what you got and then we bring together and then we can take a subset from there and simply rank them using our reciprocal rank fusion function remember that we created over here to perform the calculation. So let's see what that looks like. So if I now take my query where should I park my rainy day funds and I'm actually going to walk you I'm I'm going to walk you through it over here. Let me see. Let's do like this. Um I should do that one by one. So let's first do we get K case K is 10. This is really why I love the interactive uh window because this is ultimately the best way to learn because I can run this all at once and immediately show you the results. But if I run this over here, you can see let's first focus on the BM25 search. So here's the BM25 search and here are the IDs. Remember K is 50. So we get 50 documents back. Then let's do the dense search. So here we get the dense ids also 50 documents that we get back. Now here we're going to perform the rank fusion by putting the two ID lists in here. So you can see we put that into an actual list. So we just stack them together. So you can see here's the first 50 and then here is here starts the second list. So that is the input and then if you go over here you'll find that this takes a list with a list of strings which is exactly the format that we have and that's how it performs the calculations. So that is like walking through it step by step. If we then run this and show everything now we can see everything coming together. So let me show you what this looks like. So here we can see that for our original query that we've been using over and over again. Here's what BM25 will give you back as top five. Here is what dense will do. And then here is the hybrid with RF. So here we can already start to see these differences, right? And just so you know, right now we have no way really yet of telling or seeing what is correct and what is incorrect. We can only do that once we get to the evaluation and we map the original query back to the original documents. Right? I think we checked this in the beginning, but I don't remember which ids actually belong to this query. But you can see for example that uh this one really stands out over here. You have that douff deer over here which is not in BM25. And then you can see with RF this is moved a little bit to the top. And you can see uh that starting with in addition this one this is now actually ranked number one. And I could see why because you could see it is number two for BM25 and also number two for um for dense. So both have a very high rank in uh in both results and that's why it's kind of like pushed up. And then the bonds you can see it ranks over here BM25 but it's not in the dense so that's why that moved down. So this is how this is now all coming together and we get a fused ranking of the two. All right so that was step number three of how we can bring the results together. Now there's going to be one more final step and that is we're going to add the re-ranker. So what we're going to do over here is if you look at the process of like the dense embeddings that is uh what you would call a buy encoder. So both the query and the corpus are they they go through the vectorization process uh in in separate paths when using a cross encoder we can do that at the same time. So the query can go through the encoder together with all of the all of the documents. And let me actually show you over here again what that looks like without going too deep into everything. What a reranker essentially does is it looks at all of the results that are retrieved and now also already combined using RFF and then it's going to reorder them based on actually looking at okay but now wait what is the question and what are the actual documents that I have and going through this and there are various models that we can use for this you can use open source models but I'm going to use one of the one of the best re-ranked models out there and that is a model from co here. You can set up an account on co here and you can use the free API key because they have a free plan available to follow along. This is what I would recommend but you could also swap it out for an open source one. It is explained over here uh what that would look like. So we're going to use rerank fe uh fast. So let's show what that looks like. And this is actually now also a very simple setup because we've done most of the work already. So let's go into the rerank file. You can see that right now we set the model and we use the cohair client we get all of our retrievers and then the only thing that we do is beyond the hybrid candidates. So this line over here is exactly what we did with the with the previous step, right? to rank them using RFF. Now, all we do is we take that same list and we add a reranker on top of it and then we showcase everything. So, let's walk through this step by step again so you can get a feel for what this looks like. We'll load we'll load everything. Now, let's see. Let's go into the search here and then the show again. Let's take our beautiful query again. Okay. So, let's do the same trick, right? We say K is 10, candidates is 50. So now if we do this these are our 50 candidates and remember here RFF is already applied. So what we did um coming back we should come back to so we did BM25 search then search then reciprocal rank fusion which is RRF by the way and just noticed that for the past like couple couple of examples I said RFF I think sorry about that. So now with this one little line over here, we perform all of these and now we add the re-ranking on top of that. So here is how that works. You can now see all of the candidate ids that we have. So we have all of the ids and then we have the texts. So we get back to okay, what are all the actual documents that were retrieved? And now this is where the magic happens. We do coer, we do the rerank, we put in the model, and now we put in a query. query same candidate text over here top end we set that to 10 in this case so let's see what we get out of that so this is another API call that we make and now you can see we get a reranked version of it 10 results and also some scores in here and we let that return to uh we let the function return all of that so now that we went through step by step let me run everything. So we can get again to let's see to a top five. So now you can see if we do the RRF only the hybrid these are the documents that get retrieved. And now if we do hybrid plus the reranking you can see that it's different. So this is now on number one. You can see that duftier again which through RF was on number two but now it's on number five over here. So you can see all of these differences are very subtle and we are constantly looking at the top five. But what you should always keep in mind is if you scale this to many questions, many documents in production environments also with I don't know top 10, top 20, this is actually going to make a difference because the difference between a rack system like kind of like not working or sometimes working is all about which documents show up when you ask which which question. And these subtle nuances, they make all the difference. And that's how you're going to optimize your system. So that's it may kind of like feel very repetitive and it's like, yeah, it all kind of like got get we all kind of like get the same results, right? But we don't. And that's why now we can actually go to the last file, evaluate. So if you made it up until this point, great job. We're almost there because now we're going to perform the evaluation to see if what we're doing actually makes sense. So coming back to let's see our documents over here. Let me come to the NDCG. So normalized discounted cumulative gain. So this is in information retrieval one of the golden standards to measure retrieval quality. Again, there is some math behind it to better understand this, but we again look at the rank of the retrieved documents and then whether they're relevant or not. And then we compute a score between zero and one. And this is how we can compare different iterations. So what you should keep in mind here is if we run this higher means better score. We change something. We try a different embedding model. We try a different chunking strategy. We try a different re-ranker. We try a different step. Higher score is better. Lower score worse. That's how we're going to optimize this. So how I have implemented that for you? Again, you can see the implementation in pure Python over here. I'm not going to going to go through everything, but this is how we can calculate it. And if I now scroll down and uh pretty much well let's go let's go through this step by step because we have the function and then we get we load all of the queries first. So let's get our entire corpus ready. So now we have everything ready. Now we have these the retrievers. So the here's where we're going to wire up the retrieval mechanisms, right? And let's see, we have some other helper functions in here. And this is where let's see we're actually going to run everything. And I'm simply just going to do this and then we're going to reverse engineer because as you can see what we're now going to do is for each query that we have within our uh data set and by the way we limit it to uh let's see we limit it to 50 in in this case. So I'm going to take a random sample of 50 uh queries and then we're going to run it to that. So, I'm not going to do the full uh full corpus size. So, you can play with this. And pretty much uh if you look at where we're setting that up, you can see over here, we're taking a random sample. So, here this is this is 50 and we set a random seat of 42. Meaning every time you run this, the uh exact slice of the data is exactly the same. Okay. So, now this is completed. And why I do 50 in this case just to kind of like speed things up and this will also put you within the uh within the free plan of of go here that you can still use it I believe without running into API errors. So okay but now we have some numbers over here. So let's look at them and check what's actually the differences because here you can really see the impact of what we're building. So you can see that for our score and remember um a score between zero uh and one is what the NGCG at 10 will give you but we multiplied it by uh 10 or sorry by 100 to kind of like get a get the percentage in there. This is also usually how it's reported within all of the research papers. So if you see if you look at if we only do BM25 we get a score of 28 which is not that great and that is because this particular data set that we're using is known to not be very effective with direct keyword-based matching. Then if you look at dense you can see that clearly performs better. So the the vector embeddings that is already a lot better at retrieving the right documents. Now you can see if we then combine that using RRF you can see that we get we we we are somewhat in the middle. So between this two but now here is where it gets interesting. If we now take the hybrid and we rerank it all of a sudden we jump up to 47. That is a major increase over uh dense alone really while just literally adding one simple rerank step because remember also from your codebase uh over here this is literally all that we add to it that is what we add to it and because of that we jump to this particular score. So that is how you can evaluate your methods and your approaches. And now one the most important thing to keep in mind is do you always need this entire stack? And the answer is no. So you should figure out for your corpus what the best combination of this is. And I've now showed you an experimental setup that you can use to do this because in this case actually interesting. It could be that maybe if we take BM25 out of it and we just do dense and we maybe increase the retrieval uh or the the top K we we increase that in here and then apply the ranker we may even get higher we may even get to 50. So this is now the experimental part that you need to get into. And that's I think one of the main benefits of like going through this entire video and really trying to understand it rather than just like copying samples that you find online or letting cloud code oneshot it is understanding the mechanisms behind it. And what's let's see what I want to show right now is finally now that we're coming to the end of this project is how you can apply this to your own project and the setup is like the from a code perspective it's almost exactly the same. So you decide what kind of documents do you want to use, right? Are they notion, Zenex, whatever. Take your data, check if you need to chunk. So um you like you ideally want to chunk uh first of all, you have a hard limit of what you can actually send into uh the embedding model. But even like in in most like best practices, you want to everything over like 1,000 tokens, see if you can already split it up, for example. So this is a step we did not have to do in this project because the corpus was already like there was small isolated documents, right? Okay. Then you want to pick an embedding model. Then you're going to build the indexes, wire everything up, and then you're going to build the evaluation data set. And this is the most important gap that you'll probably encounter within your own project. And that's why I created an entire guide for this as well because we used the bear style uh evaluation set. Remember, right? And that's why we have this curries relationships and corpus. And that's why we can run this. That's why we can say anything meaningful about the adjustments that we make in the real world. You don't have this. So then the question is okay, but how do I improve my system beyond just kind of like vibes and just like running it how pretty much like 90 probably even more people like working with AI are doing it. just chuck everything into the factor database and like let just just let it run and then just like check off of wipes. This is how you get like scientific about this. So how do you create that? Well, let's see. Coming back to the guides, this is the one I created a guide over here on how to do that. But the thing is it's it's it's pretty simple. You already have the documents. That should be your starting point. And then you're going to use an LLM with a prompt like this. You are a user with a question. The following document uh contains the answer contains the answer. Generate a single realistic question that a user would ask in their own word. So rules question must be answerable by the document and you can loop over this. So you can even use like a small cheap model for this. And now you can exactly structure your own data set with document ids and questions and relationships in the format that we used in this tutorial. And in that way you can start to improve it over time, add more to it. And this is just super simple nowadays with with LLMs. So that's how you build your own eels really and how you really put this into practice beyond just the tutorial. All right. Right. And with that, we've come to the end of this video. Now, as always, if you found it valuable, please leave a like and also consider subscribing. And if you want to go really deep on AI engineering and you love tutorials and lectures like this, actually covering what works in production and not just like most of the AI crap that's out there online, then make sure to check out the Genai Accelerator. It's a six-week track where I pretty much transition you from being a general developer into an actual AI engineer by showing you exactly how we build and deliver all of the client projects for data luminina, our AI development company where we've been working on AI projects for close to a decade already. So, if that sounds interesting, make sure to check it out. And then if you want to continue learning here on YouTube, I'm going to link this video up here, which is another approach to information retrieval, but then using an agentic approach. So how to go about agentic rag is what I explain in this video. So I also highly recommend to check out